[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dragkraft"
version = "0.1.0"
description = "Python refactor of the legacy MATLAB Dragkraft train simulation."
readme = "PROJECT_DOCUMENTATION_AND_REFACTOR_PLAN.md"
requires-python = ">=3.11"
dependencies = [
  "matplotlib",
  "numpy",
  "openpyxl",
  "pandas",
]

[project.optional-dependencies]
dev = [
  "pytest",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = ["-p", "no:cacheprovider"]

[tool.setuptools.packages.find]
where = ["."]
include = ["dragkraft*"]
